{
  "dependencies": {
    "axios": "^1.4.0",
    "concurrently": "^8.2.0",
    "json-server": "^0.17.3"
  },
  "scripts": {
    "start:db": " npx json-server -w db.json",
    "start:parcel": "npx parcel index.html",
    "start": "concunpm:start:*"
  },
  "devDependencies": {
    "buffer": "^6.0.3"
  }
}
